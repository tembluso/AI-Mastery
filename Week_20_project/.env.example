# Example environment file for RAG Tweet Assistant

# Required: your OpenAI API key (never commit the real one)
OPENAI_API_KEY=sk-yourkeyhere

# Optional overrides
EMBED_MODEL=text-embedding-3-small
CHAT_MODEL=gpt-4o-mini
